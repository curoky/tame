name: bison
website: https://www.gnu.org/software/bison/
description:

version:
  - 3.1

archive: http://ftp.gnu.org/gnu/bison/bison-{{version}}.tar.gz
git:

depend:

build:
  type: configure
  build_path: _build
  step:
    - "{{repo_path}}/configure --prefix={{install_path}}"
    - "make -j {{thread_num}}"
    - "make install"
