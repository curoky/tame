name: libev
website: http://software.schmorp.de/pkg/libev.html
description:

version:
  - 4.25

download:
  archive-url: http://dist.schmorp.de/libev/libev-{{version}}.tar.gz
  git-url:

depend:
  lib:
  tool:

build:
  type: configure
  build_path: _build
  step:
    - '{{repo_path}}/configure --prefix={{install_path}}'
    - 'make -j {{thread_num}}'
    - 'make install'
