name: tree
website:
description:

version:
  - 1.8.0

archive: http://mama.indstate.edu/users/ice/tree/src/tree-{{version}}.tgz
git:

depend:

build:
  type: custom
  build_path: .
  step:
    - "make"
    - "mkdir -p {{install_path}}/bin"
    - "cp {{repo_path}}/tree {{install_path}}/bin/tree"
