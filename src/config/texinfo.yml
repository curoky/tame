name: texinfo
website:
description:

version:
  - 6.6

archive: http://ftp.gnu.org/gnu/texinfo/texinfo-{{version}}.tar.gz
git:

depend:

build:
  type: configure
  build_path: _build
  step:
    - "{{repo_path}}/configure --prefix={{install_path}}"
    - "make -j {{thread_num}}"
    - "make install"
