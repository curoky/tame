website: https://www.gtk.org/
description:

version:
  - 2.61

archive: https://ftp.gnome.org/pub/gnome/sources/glib/{{version}}/glib-{{version}}.0.tar.xz

depend:
  - zlib
  - util-linux

compile:
  type: ninja
  args: --buildtype=release
