target:
  - type: build
    retriever:
      type: http
      url: https://github.com/cquery-project/cquery/archive/master.zip
    tool:
      type: cmake
      # args: -DCMAKE_EXPORT_COMPILE_COMMANDS=YES -DCMAKE_BUILD_TYPE=Release

depend:
  - clang
