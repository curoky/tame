website:
description:

version:
  - 3.7.3

archive: https://www.python.org/ftp/python/{{version}}/Python-{{version}}.tgz

depend:
  - lzma # option deps

compile:
  type: configure
  args: # --enable-optimizations
