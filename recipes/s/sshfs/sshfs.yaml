website:
description: A network filesystem client to connect to SSH servers

version:
  - 3.5.2

git: git@github.com:libfuse/sshfs.git
archive: https://github.com/libfuse/sshfs/releases/download/sshfs-{{version}}/sshfs-{{version}}.tar.xz

depend:
  - libfuse
  - glib

compile:
  type: ninja
