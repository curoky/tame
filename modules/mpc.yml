name: mpc
website:
description:

version:
  - 1.1.0

archive: http://ftp.gnu.org/gnu/mpc/mpc-{{version}}.tar.gz
git:

depend:
  - gmp
  - mpfr

build:
  type: configure
  build_path: _build
  step:
    - "{{repo_path}}/configure --prefix={{install_path}}"
    - "make -j {{thread_num}}"
    - "make install"
