name: fcitx5-qt
website: https://gitlab.com/fcitx/fcitx5-qt.git
description: Qt library and IM module for fcitx5

version:
  - master

archive: https://gitlab.com/fcitx/fcitx5-qt/-/archive/master/fcitx5-qt-master.tar.gz
# git: git@gitlab.com:fcitx/fcitx5-qt.git
git:

depend:
  - fcitx5

build:
  type: cmake
  build_path: _build
  step:
    - 'cmake -B {{repo_path}}/_build -S {{repo_path}}/. -DCMAKE_PREFIX_PATH="{{cmake_prefix}}" -DCMAKE_INSTALL_PREFIX={{install_path}} -DENABLE_QT4=OFF'
    - "make -j {{thread_num}}"
    - "make install"
