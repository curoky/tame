name: gmp
website: https://gmplib.org/
description:

version:
  - 6.1.2

archive: http://ftp.gnu.org/gnu/gmp/gmp-{{version}}.tar.xz
git:

depend:

build:
  type: configure
  build_path: _build
  step:
    - "{{repo_path}}/configure --prefix={{install_path}}"
    - "make -j {{thread_num}}"
    - "make install"
