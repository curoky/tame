website: http://www.zsh.org
description:

version:
  - 5.6.2
  - 5.7.1

archive: https://jaist.dl.sourceforge.net/project/zsh/zsh/{{version}}/zsh-{{version}}.tar.xz

depend:
  - ncurses

### ERROR 1
# checking if POSIX sigsuspend() works... no
# checking if tcsetpgrp() actually works... error
# configure: error: unexpected return status
# CRITICAL	:builder: build error with 1
# slove : add arg `--with-tcsetpgrp` to configure
compile:
  type: configure
  build_path: .
  args: --with-tcsetpgrp --with-term-lib="ncurses"
